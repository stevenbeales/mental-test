build: 
  config: 
    - RACK_ENV: production
  languages: 
    - ruby
release: 
  command: 
    - gem update bundler
    - bundle exec rake db:migrate
run: 
  web: bundle exec puma -C config/puma.rb
